<!DOCTYPE html>

<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Carnivore Diet Exercise Guide - 3D Infographic</title>
    <script crossorigin src="https://cdnjs.cloudflare.com/ajax/libs/react/18.2.0/umd/react.production.min.js"></script>
    <script crossorigin src="https://cdnjs.cloudflare.com/ajax/libs/react-dom/18.2.0/umd/react-dom.production.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/babel-standalone/7.23.5/babel.min.js"></script>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div id="root"></div>

```
<script type="text/babel">
    const { useState, useEffect, useRef } = React;
    
    // Lucide React icons as inline SVG components
    const BarChart3 = ({ size = 24, ...props }) => (
        <svg width={size} height={size} viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" {...props}>
            <path d="M3 3v18h18"/><rect width="4" height="7" x="7" y="10" rx="1"/><rect width="4" height="12" x="15" y="5" rx="1"/>
        </svg>
    );
    
    const Dumbbell = ({ size = 24, ...props }) => (
        <svg width={size} height={size} viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" {...props}>
            <path d="m6.5 6.5 11 11"/><path d="m21 21-1-1"/><path d="m3 3 1 1"/><path d="m18 22 4-4"/><path d="m2 6 4-4"/><path d="m3 10 7-7"/><path d="m14 21 7-7"/>
        </svg>
    );
    
    const Activity = ({ size = 24, ...props }) => (
        <svg width={size} height={size} viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" {...props}>
            <path d="M22 12h-4l-3 9L9 3l-3 9H2"/>
        </svg>
    );
    
    const Calendar = ({ size = 24, ...props }) => (
        <svg width={size} height={size} viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" {...props}>
            <rect width="18" height="18" x="3" y="4" rx="2" ry="2"/><line x1="16" x2="16" y1="2" y2="6"/><line x1="8" x2="8" y1="2" y2="6"/><line x1="3" x2="21" y1="10" y2="10"/>
        </svg>
    );
    
    const AlertCircle = ({ size = 24, ...props }) => (
        <svg width={size} height={size} viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" {...props}>
            <circle cx="12" cy="12" r="10"/><line x1="12" x2="12" y1="8" y2="12"/><line x1="12" x2="12.01" y1="16" y2="16"/>
        </svg>
    );
    
    const TrendingUp = ({ size = 24, ...props }) => (
        <svg width={size} height={size} viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" {...props}>
            <polyline points="22 7 13.5 15.5 8.5 10.5 2 17"/><polyline points="16 7 22 7 22 13"/>
        </svg>
    );

    const Carnivore3DInfoGraphic = () => {
      const [activeLesson, setActiveLesson] = useState(0);
      const [rotation, setRotation] = useState(0);

      useEffect(() => {
        const interval = setInterval(() => {
          setRotation(prev => (prev + 0.5) % 360);
        }, 50);
        return () => clearInterval(interval);
      }, []);

      const lessons = [
        {
          title: "Needs Analysis",
          icon: BarChart3,
          color: "#ef4444",
          steps: ["Evaluate daily activities", "Note physical metrics", "Set realistic goals"],
          key: "Individual foundation"
        },
        {
          title: "Intensity vs Volume",
          icon: TrendingUp,
          color: "#f97316",
          steps: ["High intensity, low exposure", "2-3 sessions/week", "20-30 min maximum"],
          key: "Quality over quantity"
        },
        {
          title: "Gender Considerations",
          icon: Activity,
          color: "#eab308",
          steps: ["Universal principles", "Individual adjustments", "Hormonal awareness"],
          key: "Personalized approach"
        },
        {
          title: "Resistance Training",
          icon: Dumbbell,
          color: "#22c55e",
          steps: ["Weights/bands/bodyweight", "Near exhaustion", "Multi-day recovery"],
          key: "Cornerstone for longevity"
        },
        {
          title: "Common Concerns",
          icon: AlertCircle,
          color: "#3b82f6",
          steps: ["Incidental vs training", "Avoid overtraining", "Balance enjoyment"],
          key: "Smart recovery"
        },
        {
          title: "Weekly Routine",
          icon: Calendar,
          color: "#8b5cf6",
          steps: ["Day 1,3,6: Resistance", "Day 2,4,5,7: Rest/light", "Focus on recovery"],
          key: "Sustainable structure"
        }
      ];

      const WeeklySchedule = () => {
        const days = [
          { day: "Mon", type: "resistance", intensity: 90 },
          { day: "Tue", type: "rest", intensity: 10 },
          { day: "Wed", type: "resistance", intensity: 85 },
          { day: "Thu", type: "rest", intensity: 15 },
          { day: "Fri", type: "rest", intensity: 20 },
          { day: "Sat", type: "resistance", intensity: 88 },
          { day: "Sun", type: "rest", intensity: 0 }
        ];

        return (
          <div className="grid grid-cols-7 gap-2 mb-6">
            {days.map((d, i) => (
              <div key={i} className="text-center">
                <div className={`h-24 rounded-lg flex flex-col items-center justify-center ${
                  d.type === 'resistance' ? 'bg-gradient-to-b from-red-500 to-red-600' : 'bg-gradient-to-b from-blue-400 to-blue-500'
                } text-white shadow-lg transform hover:scale-105 transition-transform`}>
                  <div className="text-xs font-bold mb-1">{d.day}</div>
                  <div className="text-2xl font-bold">{d.intensity}%</div>
                  <div className="text-xs mt-1">{d.type === 'resistance' ? '20-30min' : 'Recovery'}</div>
                </div>
              </div>
            ))}
          </div>
        );
      };

      const Cube3D = ({ lesson, index, isActive }) => {
        const Icon = lesson.icon;
        const angle = (index * 60) + rotation;
        const radius = 120;
        const x = Math.cos(angle * Math.PI / 180) * radius;
        const z = Math.sin(angle * Math.PI / 180) * radius;
        const scale = 1 + (z / radius) * 0.3;
        const opacity = 0.5 + (z / radius) * 0.5;
        
        return (
          <div
            onClick={() => setActiveLesson(index)}
            style={{
              transform: `translateX(${x}px) scale(${scale})`,
              opacity: opacity,
              zIndex: Math.round(z + 100)
            }}
            className={`absolute left-1/2 top-1/2 -ml-16 -mt-16 w-32 h-32 cursor-pointer transition-all duration-300 ${
              isActive ? 'ring-4 ring-white' : ''
            }`}
          >
            <div 
              className="w-full h-full rounded-xl shadow-2xl flex flex-col items-center justify-center text-white transform hover:scale-110 transition-transform"
              style={{ 
                background: `linear-gradient(135deg, ${lesson.color} 0%, ${lesson.color}dd 100%)`,
                boxShadow: `0 20px 40px ${lesson.color}40`
              }}
            >
              <Icon size={32} strokeWidth={2.5} />
              <div className="text-xs font-bold mt-2 text-center px-2">{lesson.title}</div>
            </div>
          </div>
        );
      };

      const current = lessons[activeLesson];

      return (
        <div className="w-full min-h-screen bg-gradient-to-br from-gray-900 via-gray-800 to-gray-900 text-white p-8 overflow-auto">
          <div className="max-w-6xl mx-auto">
            <div className="text-center mb-8">
              <h1 className="text-5xl font-bold mb-2 bg-gradient-to-r from-red-500 via-orange-500 to-yellow-500 text-transparent bg-clip-text">
                Carnivore Diet Exercise Guide
              </h1>
              <p className="text-gray-400 text-lg">High Intensity • Low Volume • Maximum Recovery</p>
            </div>

            <div className="relative h-80 mb-12">
              {lessons.map((lesson, index) => (
                <Cube3D 
                  key={index} 
                  lesson={lesson} 
                  index={index}
                  isActive={activeLesson === index}
                />
              ))}
            </div>

            <div className="bg-gray-800 rounded-2xl p-8 shadow-2xl mb-8 border border-gray-700">
              <div className="flex items-center gap-4 mb-6">
                <div 
                  className="w-16 h-16 rounded-xl flex items-center justify-center shadow-lg"
                  style={{ backgroundColor: current.color }}
                >
                  <current.icon size={32} />
                </div>
                <div>
                  <h2 className="text-3xl font-bold">{current.title}</h2>
                  <p className="text-gray-400 italic">{current.key}</p>
                </div>
              </div>

              <div className="grid md:grid-cols-3 gap-4 mb-6">
                {current.steps.map((step, i) => (
                  <div key={i} className="bg-gray-700 rounded-lg p-4 border-l-4" style={{ borderColor: current.color }}>
                    <div className="text-2xl font-bold mb-2" style={{ color: current.color }}>{i + 1}</div>
                    <p className="text-sm">{step}</p>
                  </div>
                ))}
              </div>
            </div>

            <div className="bg-gray-800 rounded-2xl p-8 shadow-2xl border border-gray-700">
              <h3 className="text-2xl font-bold mb-6 flex items-center gap-3">
                <Calendar size={32} className="text-purple-500" />
                Sample Weekly Routine
              </h3>
              <WeeklySchedule />
              
              <div className="grid md:grid-cols-2 gap-6 mt-6">
                <div className="bg-gradient-to-br from-red-900 to-red-800 rounded-lg p-6">
                  <h4 className="font-bold text-xl mb-3 flex items-center gap-2">
                    <Dumbbell size={24} />
                    Resistance Days
                  </h4>
                  <ul className="space-y-2 text-sm">
                    <li>• 5 min warm-up</li>
                    <li>• 3-4 exercises at 80-90% max</li>
                    <li>• 6-8 reps, 2-3 sets</li>
                    <li>• 20-30 min total</li>
                  </ul>
                </div>
                
                <div className="bg-gradient-to-br from-blue-900 to-blue-800 rounded-lg p-6">
                  <h4 className="font-bold text-xl mb-3 flex items-center gap-2">
                    <Activity size={24} />
                    Recovery Days
                  </h4>
                  <ul className="space-y-2 text-sm">
                    <li>• Complete rest or light activity</li>
                    <li>• Optional 20-30 min walk</li>
                    <li>• Focus on sleep quality</li>
                    <li>• High protein carnivore meals</li>
                  </ul>
                </div>
              </div>
            </div>

            <div className="grid md:grid-cols-3 gap-4 mt-8">
              <div className="bg-gradient-to-br from-emerald-600 to-emerald-700 rounded-xl p-6 text-center">
                <div className="text-4xl font-bold mb-2">2-3x</div>
                <div className="text-sm">Sessions per week</div>
              </div>
              <div className="bg-gradient-to-br from-orange-600 to-orange-700 rounded-xl p-6 text-center">
                <div className="text-4xl font-bold mb-2">20-30</div>
                <div className="text-sm">Minutes per session</div>
              </div>
              <div className="bg-gradient-to-br from-purple-600 to-purple-700 rounded-xl p-6 text-center">
                <div className="text-4xl font-bold mb-2">80-90%</div>
                <div className="text-sm">Maximum effort</div>
              </div>
            </div>

            <div className="flex justify-center gap-2 mt-8">
              {lessons.map((_, i) => (
                <button
                  key={i}
                  onClick={() => setActiveLesson(i)}
                  className={`w-3 h-3 rounded-full transition-all ${
                    activeLesson === i ? 'w-8 bg-white' : 'bg-gray-600'
                  }`}
                />
              ))}
            </div>
          </div>
        </div>
      );
    };

    ReactDOM.render(<Carnivore3DInfoGraphic />, document.getElementById('root'));
</script>
```

</body>
</html>
